% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{plot.bbqr}
\alias{plot.bbqr}
\title{Trace and density plots for a fitted binary quantile regression}
\usage{
\method{plot}{bbqr}(x, which = NULL, type = c("both", "trace", "density"), ...)
}
\arguments{
\item{x}{An object of class \code{"bbqr"}.}

\item{which}{Which coefficients to plot, by name or index. Index 1 is the
intercept. Defaults to all.}

\item{type}{\code{"trace"}, \code{"density"}, or \code{"both"} (the default), which draws
the two side by side.}

\item{...}{Passed to the underlying plotting calls.}
}
\value{
\code{x}, invisibly. Called for its side effect.
}
\description{
Trace and density plots for a fitted binary quantile regression
}
\examples{
set.seed(1)
n <- 150
X <- matrix(rnorm(n * 3), n, 3, dimnames = list(NULL, c("x1", "x2", "x3")))
y <- as.numeric(X \%*\% c(1, -1, 0) + rnorm(n) > 0)
fit <- bbqr(y ~ x1 + x2 + x3, data = data.frame(y = y, X),
            ndraw = 500, burn = 100)
op <- graphics::par(no.readonly = TRUE)
plot(fit, which = "x1")
graphics::par(op)
}
