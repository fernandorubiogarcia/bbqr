% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-continuous.R
\name{predict.cbqr}
\alias{predict.cbqr}
\title{Predictions from a continuous quantile regression}
\usage{
\method{predict}{cbqr}(object, newdata = NULL, interval = FALSE, level = 0.95, ...)
}
\arguments{
\item{object}{An object of class \code{"cbqr"}.}

\item{newdata}{A data frame of predictors. Required: the fitted object does
not store the design matrix.}

\item{interval}{Return posterior credible intervals for the fitted quantile
as well as the point estimate. Uses the full retained draws, so the cost is
\code{nrow(newdata) * ndraw_kept} doubles.}

\item{level}{Credible level when \code{interval = TRUE}.}

\item{...}{Ignored.}
}
\value{
A numeric vector, or a matrix with columns \code{fit}, \code{lower}, \code{upper}
when \code{interval = TRUE}.
}
\description{
Returns the fitted conditional quantile of the response, which is the
linear predictor. That is a different quantity from what \code{\link[=predict.bbqr]{predict.bbqr()}}
returns -- \code{P(y = 1 | x)} from the ALD distribution function -- which on a
continuous fit saturates at 1 and is silently meaningless.
}
