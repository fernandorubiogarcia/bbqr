% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prior.R
\name{.bbqr_b0_reference}
\alias{.bbqr_b0_reference}
\title{The tau-calibrated reference intercept prior}
\usage{
.bbqr_b0_reference(quantile)
}
\arguments{
\item{quantile}{The quantile level \code{tau}, in \verb{(0, 1)}.}
}
\value{
A list with elements \code{mean} and \code{var}.
}
\description{
At the \code{sigma = 1} anchor and with \verb{x'beta = 0}, the model reduces to
\code{Pr(y = 1) = G_tau(beta0)} with \code{G_tau} the distribution function of \code{-E}
for \code{E ~ ALD(0, 1, tau)}. By the probability integral transform,
\code{G_tau(beta0)} is exactly \code{Uniform(0, 1)} when \code{beta0 ~ ALD(0, 1, 1 - tau)},
so the reference normal prior is that law's moment match:
}
\details{
\verb{mean = -theta = -(1 - 2 tau) / (tau (1 - tau))}
\verb{var  = (1 - 2 tau + 2 tau^2) / (tau^2 (1 - tau)^2)}

The mean is not zero away from the median. Centring the intercept prior at
the origin puts the prior-predictive mean of \code{Pr(y = 1)} at 0.73 rather than
0.5 at \code{tau = 0.1}, so it is a specification error rather than a neutral
default. The variance is within 5\% of the value that minimises the
Kolmogorov-Smirnov distance to \code{Uniform(0, 1)} over scalings of itself, so
no optimisation is performed: the closed form is used as it stands.
}
\keyword{internal}
