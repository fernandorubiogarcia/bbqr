% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{predict.bbqr}
\alias{predict.bbqr}
\title{Predicted probabilities and indices from a binary quantile regression}
\usage{
\method{predict}{bbqr}(object, newdata = NULL, type = c("response", "link", "class"), ...)
}
\arguments{
\item{object}{An object of class \code{"bbqr"}.}

\item{newdata}{A data frame of covariates. Required: fitted objects do not
retain the design matrix, so that they stay small enough to hold thousands
of fits in memory during a simulation study.}

\item{type}{\code{"response"} returns \eqn{P(y = 1 \mid x)} implied by the ALD at
the fitted quantile; \code{"link"} returns the latent index
\eqn{\beta_0 + x'\beta}; \code{"class"} returns the 0/1 label given by the sign
of that index.}

\item{...}{Ignored.}
}
\value{
A numeric vector, one entry per row of \code{newdata}.
}
\description{
Predicted probabilities and indices from a binary quantile regression
}
\details{
Predictions use the posterior mean coefficients. Because the coefficient
vector is identified only up to a positive scale, \code{type = "response"}
depends on the identification anchor, whereas \code{type = "class"} does not.
}
\examples{
set.seed(1)
n <- 150
X <- matrix(rnorm(n * 3), n, 3, dimnames = list(NULL, c("x1", "x2", "x3")))
y <- as.numeric(X \%*\% c(1, -1, 0) + rnorm(n) > 0)
d <- data.frame(y = y, X)
fit <- bbqr(y ~ x1 + x2 + x3, data = d, ndraw = 500, burn = 100)
head(predict(fit, newdata = d, type = "response"))
mean(predict(fit, newdata = d, type = "class") == y)
}
