% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-continuous.R
\name{cbqr}
\alias{cbqr}
\title{Bayesian quantile regression for a continuous response}
\usage{
cbqr(
  formula,
  data = NULL,
  quantile = 0.5,
  penalty = c("alasso", "lasso", "none"),
  ndraw = 10000,
  burn = NULL,
  keep = 1,
  prior = NULL,
  q = NULL,
  standardize = TRUE,
  use_boundaries = FALSE,
  clip = NULL,
  sampler = NULL,
  beta_init = NULL,
  beta0_init = 0
)
}
\arguments{
\item{formula}{A two-sided formula. The intercept is split off and never
penalised, so it must not appear among the penalised columns.}

\item{data}{Optional data frame.}

\item{quantile}{Quantile level(s) in (0, 1).}

\item{penalty}{One of \code{"alasso"}, \code{"lasso"}, \code{"none"}.}

\item{ndraw, burn, keep}{MCMC controls. \code{ndraw} must be divisible by \code{keep}.}

\item{prior}{Optional named list overriding the continuous prior
defaults. Accepted names are \code{a}, \code{b}, \code{b0_mean}, \code{b0_var}, \code{mh_sd0},
\code{target_acc}, \code{sigma_init}, \code{delta_init}, \code{alpha_delta}, \code{beta_delta},
(note \code{mh_sd0} defaults to 0.5 here, not the binary 0.05: \code{delta} mixes
over a wider range once \code{sigma} is live)
and, by penalty, \code{omega_init}/\code{alpha_omega}/\code{beta_omega}
(\code{"alasso"}), \code{tau_init}/\code{alpha_tau}/\code{beta_tau} (\code{"lasso"}) or
\code{beta_var} (\code{"none"}). This is \strong{not} a \code{\link[=prior]{prior()}} object:
that constructor builds the binary hierarchy and is rejected here.}

\item{q}{Exponent on sigma in the local-scale prior. Layer-specific default:
1 for \code{"alasso"}, 0 for \code{"lasso"}. Supplying it with \code{penalty = "none"}
is an error: there is no penalty layer for the scale to enter.}

\item{standardize}{Centre and scale \strong{both} \code{X} and \code{y}. Defaults to \code{TRUE},
which is not the binary default: no proper hierarchy is
exactly response-scale equivariant, so fixing the scale is part of the
specification rather than a preprocessing convenience. Draws are mapped
back to the original units exactly.}

\item{use_boundaries}{Apply the kernel's numerical clamps. \code{"lasso"} and
\code{"none"} only; the adaptive-lasso kernel takes its clamps through the
per-site \code{clip} argument instead and ignores this one.}

\item{clip}{Per-site clamp configuration; \code{"alasso"} only.}

\item{sampler}{Latent-draw routine; \code{"alasso"} only.}

\item{beta_init, beta0_init}{Starting values.}
}
\value{
A \code{cbqr} object, or a \code{cbqr.list} when \code{quantile} has length > 1.
The components are:
\describe{
\item{\code{family}, \code{penalty}, \code{q}, \code{quantile}}{what was fitted.}
\item{\code{beta}, \code{beta0}, \code{sigma}}{the kept draws: a matrix of slopes, and
vectors for the intercept and the ALD inverse-scale. Unlike a binary
fit, \code{sigma} is always a live parameter here.}
\item{\code{lambdasq}, \code{omega}, \code{delta}}{the shrinkage layer, \code{"alasso"}
only: the per-coefficient scales, the global scale, and its shape.}
\item{\code{lambdasq}, \code{tau_hyper}, \code{delta}}{the shrinkage layer, \code{"lasso"}
only: one global scale, its hyperparameter, and the shape. Absent for
\code{"none"}, which has no shrinkage layer.}
\item{\code{accept}, \code{acc}}{Metropolis acceptance rate for \code{delta}, and its
running trace. \code{NA} when there is no Metropolis step.}
\item{\code{ndraw}, \code{burn}, \code{keep}, \code{ndraw_kept}}{the MCMC controls as used.}
\item{\code{n}, \code{nvar}, \code{names}, \code{terms}, \code{call}}{the data and the model
frame the fit came from.}
\item{\code{prior}}{the resolved prior list, including any defaults filled
in.}
\item{\code{standardized}}{the centring and scaling applied to \code{X} and \code{y},
or \code{NULL}. Draws are already mapped back, so this is a record rather
than something to undo.}
\item{\code{b0_prior}}{the intercept prior actually used.}
\item{\code{clip}, \code{clip_hits}, \code{sampler}}{the clamp configuration, how often
each site fired, and the latent-draw routine. \code{"alasso"} only.}
}
}
\description{
User-facing entry point for the three continuous penalty layers. The binary
threshold model is fitted by \code{\link[=bbqr]{bbqr()}} instead; the two share no arguments
that refer to identification, because a continuous response identifies the
scale and a binary one does not.
}
\examples{
set.seed(1)
d <- data.frame(x1 = rnorm(120), x2 = rnorm(120))
d$y <- 1 + 2 * d$x1 - d$x2 + rnorm(120)
fit <- cbqr(y ~ x1 + x2, d, ndraw = 400, burn = 100)
round(coef(fit), 2)
}
\references{
Alhamzawi, R., Yu, K. and Benoit, D. F. (2012). Bayesian adaptive lasso
quantile regression. \emph{Statistical Modelling}, 12(3), 279--297.
\doi{10.1177/1471082X1101200304}

Benoit, D. F., Al-Hamzawi, R. and Yu, K. (2013). Bayesian lasso binary
quantile regression. \emph{Computational Statistics}, 28(6), 2861--2873.
\doi{10.1007/s00180-013-0407-8}

Kozumi, H. and Kobayashi, G. (2011). Gibbs sampling methods for Bayesian
quantile regression. \emph{Journal of Statistical Computation and Simulation},
81(11), 1565--1578. \doi{10.1080/00949655.2010.496117}
}
\seealso{
\code{\link[=bbqr]{bbqr()}} for the binary threshold model; \code{\link[=cbqr_alasso]{cbqr_alasso()}},
\code{\link[=cbqr_lasso]{cbqr_lasso()}} and \code{\link[=cbqr_none]{cbqr_none()}} for the direct forms; \code{\link[=predict.cbqr]{predict.cbqr()}},
\code{\link[=summary.cbqr]{summary.cbqr()}} and \code{\link[=plot.cbqr]{plot.cbqr()}} for the fitted object. \code{\link[=prior]{prior()}} does
\strong{not} configure these fitters.
}
